// Importing required namespaces
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Definition of the CheckPoint class, derived from MonoBehaviour
public class CheckPoint : MonoBehaviour
{
    // Public variable to indicate if this checkpoint is the finish line
    public bool isFinishLine = false;

    // Public variable to store the checkpoint number
    public int checkPointNumber = 1;
}